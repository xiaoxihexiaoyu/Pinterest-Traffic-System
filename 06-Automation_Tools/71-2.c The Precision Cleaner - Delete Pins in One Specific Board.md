# 有时候你不想清理整个 Pinterest 账户，只想清理一个特定的 Board

---

## 文档摘要

- **页数**: 3
- **图片**: 3
- **字数**: 485
- **Source**: 71-2.c_The_Precision_Cleaner_-_Delete_Pins_in_One_Specific_Board.pdf

---

有时候你不想清理整个 Pinterest 账户，只想清理一个特定的 Board。

### 这个版本的功能

这个 Automation：

1. 针对特定的 Board（你手动设置）
2. 使用 Pinterest 内部书签系统每次获取 250 个 Pin
3. 筛选符合条件的 Pin：
   - 发布超过 90 天 并且
   - 终身展示数为 0
4. 删除每个匹配的 Pin
5. 重复处理直到所有 Pin 都被处理，每个 Board 最多处理 1,250 个 Pin（如果调整循环，可以更多）
6. 错误处理（如果 Pinterest API 触发太快，会暂停 100 秒）

### 如何设置和使用这个 Automation？

下载以下场景 blueprint：

The Precision Cleaner - Delete Pins in One Board.blueprint.json(69.4 KB)

按照"3. How to Set It Up in make.com"章节中的说明，将场景导入 make.com。

### 选择 Pinterest Board

选择 Pinterest 账户后，你可以在第一个 Pinterest 模块中手动选择所需的 Board。最简单的方法是在搜索栏中输入部分关键词。

### 仅在需要时：更改默认参数

每个 Board 的最大 Pin 数

有两个参数定义了 Automation 可以处理的每个 Board 的最大 Pin 数

重复次数（默认值 = 5，范围 1...100） 决定每个 Board 将获取多少个 API 请求（页面）。

每次请求的 Pin 数（默认值 = 250，范围 1...250） 定义每次 API 调用检索多少个 Pin。

重要：值不得超过 250，这是 Pinterest API 的限制。

因此，默认情况下每个 Board 的最大 Pin 总数 = 5  250 = 1,250 个 Pin/Board。

如果你的 Board 有超过 1,250 个 Pin/Board，你必须增加 Repeats 到相应的值。

如果你的所有 Board 都明显少于这个数量，你可以将 Repeats 减少到较低的值。

例如，所有 Board 都少于 250 个 Pin，将 Repeats 设置为 1。场景将运行更快并使用更少的操作。

### 这个版本的优点

- 清理特定的 Board，非常适合在安全的 Board 上测试
- 允许手动控制删除位置
- 非常适合利基 Board，例如过时的产品或趋势
- 更容易调试、调整和预览

### 限制

- 一次只能处理一个 Board
- 如果想在另一个 Board 上运行，需要手动更改 Board ID

### 最佳使用场景

- 你正在测试新策略
- 你想清理季节性或过期内容
- 你正在管理多个客户 Board 并想要精细控制
- 你在扩展到所有 Board 之前正在试验新的过滤逻辑

### 想要更改过滤逻辑？

你完全可以自定义删除条件：展示数、点击数、保存数，甚至关键词。

但要小心：过滤器控制一切。

在第 4 章中学习如何安全编辑它们：Handle With Care – Advanced Filtering / 小心操作 – 高级过滤
